\ProvidesPackage{preamble}


\usepackage{hyperref}          % Clickable link
\usepackage{indentfirst}

\usepackage{polyglossia}

\usepackage[final]{microtype}

\usepackage{mathtools}    % Loads amsmath anyway
\allowdisplaybreaks
\usepackage{amssymb}
\usepackage{commath}    % Fucking abs & integrals & differentials
\usepackage{amsthm}

\usepackage{multicol}
\usepackage[no-files]{xsim}    % Without no-files, xsim clutter the folder with tex files
\xsimsetup{                    % for problem & solution separately & for each problem
  solution/print = true,
  exercise/within = section,
}

\usepackage{enumitem}

\usepackage{interval}
\intervalconfig{
  soft open fences ,    % instead of the "perverse" ]a; b]
}

\usepackage{siunitx}

\usepackage{longtable}
\usepackage{booktabs}
\usepackage{tabularx}

\usepackage{pgfplots}
\pgfplotsset{compat=1.17}

\usepackage{bookmark}

\usepackage[figure, table]{totalcount}

\usepackage[useregional]{datetime2}
\DTMsetdatestyle{ddmmyyyy}

\usepackage{float}

% Blank page https://tex.stackexchange.com/a/374542/206774
\def\blankpage{%
  \clearpage%
  \thispagestyle{empty}%
  \addtocounter{page}{-1}%
  \null%
  \clearpage}

% \usepackage{standalone}
\usepackage{subfiles} % Best loaded last in the preamble
