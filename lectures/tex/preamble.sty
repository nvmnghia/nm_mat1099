\ProvidesPackage{preamble}


\usepackage{xr}    % Reference between files
% \usepackage{xr-hyper}    % Not sure if this is needed anymore. If needed, load it before hyperref
\usepackage{hyperref}    % Clickable link
\externaldocument[chapter2:]{chapters/chapter 2/chapter 2}

\usepackage{indentfirst}

% \usepackage[vietnamese]{babel}
\usepackage{polyglossia}
\setdefaultlanguage{vietnamese}

\usepackage[final]{microtype}
\usepackage{lmodern}

\usepackage{mathtools}    % Loads amsmath anyway
\allowdisplaybreaks
\usepackage{amssymb}
\usepackage{commath}    % Fucking abs & integrals & differentials
\usepackage{xfrac}    % small inline kawaii frac
\usepackage{bm}    % Bold symbols
\usepackage{nicematrix}
\usepackage{systeme}
\usepackage{empheq}

\usepackage{amsthm}
\newtheorem{definition}{Định nghĩa}
\newtheorem{exmp}{Ví dụ}[chapter]
\newtheorem{theorem}{Định lí}[chapter]
\newtheorem{coro}{Hệ quả}[chapter]
\newtheorem{lemma}{Bổ đề}[chapter]
\newtheorem*{proposition}{Mệnh đề}
\newtheorem*{rule_of_thumb}{Quy tắc}
\newtheorem*{method}{Phương pháp}
\renewcommand{\proofname}{Chứng minh}
\renewcommand{\qedsymbol}{đpcm.}

\usepackage{enumitem}
\usepackage{multicol}
\usepackage{tasks}

\usepackage{interval}
\intervalconfig{
  soft open fences ,    % instead of the "perverse" ]a; b]
}

\usepackage{siunitx}
\sisetup{
  output-decimal-marker = {,},
  per-mode = symbol,    % m/s instead of default ms^-1
}

\usepackage{float}

\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{array}
\usepackage{hhline}

% \usepackage[table]{xcolor}    % xcolor with [table] must be loaded first, as pgfplots also loads one
\usepackage{colortbl}
\usepackage{pgfplots}
\pgfplotsset{compat=1.17}
\usetikzlibrary{decorations.markings}
\usepackage{tcolorbox}
\tcbuselibrary{theorems}

\usepackage{bookmark}

\usepackage[figure, table]{totalcount}

\usepackage[useregional]{datetime2}
\DTMsetdatestyle{ddmmyyyy}


% Blank page https://tex.stackexchange.com/a/374542/206774
\def\blankpage{%
  \clearpage%
  \thispagestyle{empty}%
  \addtocounter{page}{-1}%
  \null%
  \clearpage}


\newtcbtheorem[number within=chapter]{cdefinition}{Định nghĩa}%
{colback=blue!5, colframe=blue!50, fonttitle=\bfseries}{def}
\newtcbtheorem[number within=chapter]{ctheorem}{Định lí}%
{colback=green!5, colframe=green!50, coltitle=black!50, fonttitle=\bfseries}{thm}
\newtcbtheorem[number within=chapter]{ccoro}{Hệ quả}%
{colback=green!5, colframe=green!50, coltitle=black!50, fonttitle=\bfseries}{lemma}
\newtcbtheorem[number within=chapter]{cmethod}{Phương pháp}%
{fonttitle=\bfseries}{method}


% \usepackage{standalone}
\usepackage{subfiles} % Best loaded last in the preamble


% Show box info for debug
% https://tex.stackexchange.com/a/51763/206774
% \showboxdepth=\maxdimen
% \showboxbreadth=\maxdimen
